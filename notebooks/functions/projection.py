# to project gravity surface in local coords onto global coordinate systemimport numpy as npdef project_gravity(gzs, vs):        """    Projecting the gravity points from the local coordinate system onto the     global coordinate system.        parameters    ----------    gzs (array): gravity points     vs (array ?): vector we are projecting onto ...    which would be the surface plane (z = constant) for the global system but     represented in the local system?        returns    -------    project gz values onto the surface.    """        proj_g = (np.dot(gzs, vs) / np.l2norm(vs)) * vs        return proj_g