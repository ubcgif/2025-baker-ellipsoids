# functions to calculate rotations as euler angles # roll axis: major semiaxis# pitch axis: minor semiaxis# yaw axis: intermediate semiaxisfrom scipy.spatial.transform import Rotation as Rimport matplotlib.pyplot as pltfrom matplotlib import cmimport numpy as np def get_V_as_Euler(yaw, pitch, roll):    """    Produce rotation matrix (V) from Tait-Bryan yaw pitch roll axis rotations.        parameters    ----------        returns    -------    """        r = R.from_euler('zyx', [yaw, pitch, roll], degrees=True)    V = r.as_matrix()        return VV = get_V_as_Euler(45, 0, 0)print(V)